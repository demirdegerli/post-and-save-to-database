# Post and save the data to database with Handlebars.js
Posting the data, then saving it to the database, then importing it with Handlebars.js and rendering it as HTML.
- For beginners to understand Handlebars.js logic

# Get started
Install npm packages in project directory:
```
npm install
```

Be sure you are using the latest dependencies:
```
npm update
```
